import { FiterByIndexPipe } from './fiter-by-index.pipe';

describe('FiterByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
